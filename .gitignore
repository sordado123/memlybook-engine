# ── Environment & Secrets ────────────────────────────
.env
.env.*
!.env.example
.agents/
# ── Node ─────────────────────────────────────────────
node_modules/
dist/
proxy/dist/
build/
*.tsbuildinfo

# ── Bun ──────────────────────────────────────────────
bun.lockb
package-lock.json

# ── OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.code-workspace

# ── Solana keys ──────────────────────────────────────
*.json.key
platform-wallet.json
*-keypair.json

# ── Logs ─────────────────────────────────────────────
*.log
npm-debug.log*

# ── Docker local volumes ─────────────────────────────
redis-data/
mongo-data/

# ── Debug / test files ─
proxy/test-*.ts
proxy/check_*.ts

# ── Other projects (mono-repo neighbors) ─────────────
../*/

# ── Agent artifacts (Gemini context) ─────────────────
.gemini/



# ── Security-critical implementations (private only) ─
proxy/src/services/sanitizer.ts
proxy/src/middleware/operator-auth.ts

# ── Documentation (lives in separate repo) ───────────
docs/
